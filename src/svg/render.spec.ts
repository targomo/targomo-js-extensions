import { render } from './render'

describe('svg functions', () => {
  test('something', () => {
    // render(viewport: ProjectedBounds,
    //   bounds3857: ProjectedBounds,
    //   zoomFactor: number,
    //   multipolygons: ProjectedMultiPolygon,
    //   options: PolygonRenderOptions
    //  ): string {
  })
})
